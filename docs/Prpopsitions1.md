how the knowledge graph pic the data? from DB or from broker?


each data or event or anything must publish to mqtt broker and its name is topic
each topic is named suite to ISA-95 (you can learn what's ISA-95)
i wanna knowledge graph be live what we have in pipeline
If i turn off the program from front-end and begin a new session i wanna have a clean kg and pipeline
In the dashboard or else just the machine normalised (isa-95) apear (when the user select the machine in the connect interface)
I wanna use the KG or UNS as a one trusted source, from there any IA agent can know every thing
Can we use calculate_duration for other functions or just next to state_machine function
Delete kg_save, every pipeline added will be shown directly in kg